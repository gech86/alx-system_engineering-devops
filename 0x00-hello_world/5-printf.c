#include <stdio.h>
/**
 * main~it prints a line with printf function 
 * return :0(sign of succsess)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
		return (0);
}
